import Stox from '../Stox'

describe('Stox binary operations', () => {
  it('runs addition', () => {
    const stox = new Stox()
  })
  it.todo('runs subtraction')
  it.todo('runs multiplication')
  it.todo('runs division')
})
